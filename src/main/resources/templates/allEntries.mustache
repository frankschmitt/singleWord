{{> header}}

<h1>{{title}}</h1>

<div class="entries">

    This is just a dummy text.
    {{#entries}}
    <section>
        <header class="entry-header">
            <h2 class="entry-name"><a href="/entry/{{id}}">{{name}}</a></h2>
            <div class="entry-meta">Entered on <strong>{{addedAt}}</strong></div>
        </header>
    </section>
    {{/entries}}
</div>

{{> footer}}